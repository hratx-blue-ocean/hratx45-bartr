import React, {PureComponent} from "react";
 
export default class ItemDetailMakeOfferButton extends PureComponent {
    render() {
        return <button type="button" className="btn makeOffer">Make Offer</button>
    }
}