import React from 'react';
import { connect } from 'react-redux'
import store from './store'

const UserProfile = ({ }) => {
    return (
        <div>I am user profile</div>
    )
}
export default UserProfile;